<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Dados Cadastrados</h1>

    <% for (let dadosAbrig of locals.dadosAbrig) { %>
        <span><%= dadosAbrig.nome %></span>
        <span><%= dadosAbrig.endereco %></span>
        <span><%= dadosAbrig.cel %></span>
        <span><%= dadosAbrig.mail %></span>
        <span><%= dadosAbrig.diautil %></span>
        <span><%= dadosAbrig.fds %></span>
        <span><%= dadosAbrig.hora %></span>
        <span><%= dadosAbrig.obs %></span>
    <% } %>
</body>
</html>